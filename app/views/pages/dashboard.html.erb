<div class=space-y-12>
  <h1>Dashboard</h1>
  <p>Welcome to the Dashboard</p>

  <% if @session %>
    <p>Logged in as <%= @session.user.email_address %></p>
    <%= button_to "Logout", session_path, method: :delete %>
  <% end %>
</div>
